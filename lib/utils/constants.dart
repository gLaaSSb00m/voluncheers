import 'package:flutter/material.dart';

class AppColors {
  static const Color darkGreen = Color(0xFF014421);  // Buttons & active circle
  static const Color golden = Color(0xFFFFD700);     // Focused borders & shadows
  static const Color bgColor = Color(0xFFF1F2F6);    // Screen background
  static const Color textColor = Colors.black;        // Main text color
  static const Color lightGray = Color(0xFFDADADA);   // Inactive circle border or lines
}
